## Summary

Harness Guard 0.0.1 multi-harness content (Claude Code 5 / Codex 4 / Grok Build 4 local-posture rules, declarative engine, multi-root discovery, capabilities, Grok OSS evidence intake) is **release-ready from a safety and honesty standpoint**. No Critical product-safety invariant breaks, false confident scan outcomes, network/exec paths, or ambient-home leakage in core/tests were found. Dominant residual risks are documentation precision in `CHANGELOG.md` (overstates “clean-room” / behavior reproduction), latent multi-`tested_versions` lex-max in stale messages (unhit by current rules), and the usual Task 25 requirement to re-run full gates on the exact tag commit under owner authorization.

## Release verdict

**Ready to tag**

Content, rules, fixtures, detection, tripwires, no-egress multi-harness matrix, version `0.0.1`, capabilities goldens (5/4/4), and README/CONTEXT/agent-guide truthfulness are aligned with owner decisions of 2026-07-17 (local-posture Grok from OSS + docs; no wire claims; tripwire live-key ban lifted; canary-claim language still banned). Prior final review (`/tmp/harness-guard-sdd/final-review.md`) is re-confirmed on high-risk paths; no new Block-tag defects. Optional cheap doc polish (CHANGELOG wording, stale render_term comment) is preferred but not blocking. **Tagging remains owner-gated Task 25** — this review does not authorize `git tag`, GitHub Release, publish, or enabling freshness workflows.

## Strengths

- **Core boundary held:** `DiscoveryRoot` is the only filesystem door into core (`crates/harness-guard-core/src/discovery.rs`); `clippy.toml` still bans env/process/network APIs; ambient resolution lives only in CLI (`main.rs` `discovery_root_from_env`, including documented `GROK_HOME`).
- **Execution-free version detection is evidence-faithful:** npm package walk first; Grok managed-install fallback mirrors OSS `version_from_versioned_binary_name` + target existence without exec (`version.rs`); unit cases copied from evidence `update-version.rs`; dangling symlink → `None` → stale.
- **Vacuous-range footgun fixed:** zero-rule harness cannot report `version_in_range: true` (`scan.rs` requires non-empty rule set before claiming range); empty findings still render as “not a clean audit” (`render_term.rs`).
- **Grok rules are conservative and correctly scoped:** bool local posture only; unset → unknown; string modes (e.g. `session_metrics`) → unrecognized/unknown; `trace_upload` unset → unknown (inheritance); `evidence_class: official-documentation` only — zero `independent-reproduction` under `rules/`; limitations explicitly disclaim wire/canary claims.
- **Evidence pack honesty:** `docs/research/evidence/grok-build/2026-07-17/README.md` and protocol amendment state source-reading-only, pin channel `0.2.102`, `SOURCE_REV 124d85bc…`, semantic hashes matching `freshness/url-hashes.json`.
- **Hostile / fixture matrix:** 14 Grok cases including runtime-only managed-install symlink; symlink-config refusal; GROK_HOME redaction test; mixed two-store aggregation golden; no-egress script covers codex + claude + grok + mixed (`scripts/no-egress/run-macos.sh`).
- **Public docs mostly truthful:** README scope table, local-posture disclaimer, no cadence claims; capabilities golden pins exact counts; freshness workflows still `vars.ENABLE_FRESHNESS_WORKFLOWS == 'true'` gated.
- **Tripwires updated to owner supersession:** retired-key ban lifted with documented history constant; canary-upload claim ban remains (`tripwires.rs`).

## Issues

### Issue 1 -- Severity: suggestion
- File: `CHANGELOG.md:20-22`
- Description: Changelog says Grok rules cite “2026-07-17 **clean-room evidence**” and that “**behavior claims** are version-pinned to the **reproduced** release.” The dated intake is explicitly **source-reading only** (no network capture, no lab matrix). No behavior rules ship. README/CONTEXT/protocol are correct; the changelog alone overstates the evidence class and implies a reproduction that did not run.
- Suggestion: Reword to match protocol § Release gate / evidence README, e.g. “local-posture rules citing 2026-07-17 OSS source-reading intake; clean-room protocol documents a lab path for future behavior claims (none shipped).”
- Release impact: Fix before tag preferred
- Status: open

### Issue 2 -- Severity: suggestion
- File: `crates/harness-guard-core/src/engine.rs:246-250`
- Description: Stale-reason “max tested” uses string `.max()` over `tested.max` strings. Lexicographic max is wrong for multi-range rules (e.g. `"0.9.0"` > `"0.10.0"`). **All 13 shipped rules have a single `tested_versions` entry**, so this is latent only; `scan.rs` aggregates correctly via `parse_version` + `max_by_key`.
- Suggestion: Use the same `parse_version`/`max_by_key` approach as `conservative_aggregates` when multi-range rules land.
- Release impact: Accept for 0.0.1
- Status: open

### Issue 3 -- Severity: suggestion
- File: `freshness/last-seen.json:5-8` vs `rules/claude-code/*` / capabilities golden
- Description: Claude last-seen npm version is `2.1.202`; rules and capabilities pin verified `2.1.204`. Workflows are default-off so no production impact; enabling freshness will open a triage issue until bumped.
- Suggestion: Bump last-seen when workflows are enabled (or now as hygiene).
- Release impact: Accept for 0.0.1
- Status: open

### Issue 4 -- Severity: suggestion
- File: `rules/grok-build/*.json` (`tested_versions` with `"min": "<=0.2.102"`)
- Description: Peer MDN-style unbounded-below range claims confident pass/finding for any detected version ≤ 0.2.102. Grok’s key surface was historically contested; OSS re-documents keys at the pin. Older installs may not share the same semantics. Protocol allows `<=` for local-posture when justified; residual over-claim on ancient versions is accepted by design but is weaker for Grok than for long-stable Codex keys.
- Suggestion: Post-0.0.1 consider a tighter min (or single pin without `<=`) if key history warrants; document in limitations if desired.
- Release impact: Accept for 0.0.1
- Status: open

### Issue 5 -- Severity: suggestion
- File: `crates/harness-guard-cli/src/render_term.rs:94-95`
- Description: Comment still attributes zero findings to “grok-build until its rule work package lands.” Behavior is correct (defensive empty-rules path); attribution is historical.
- Suggestion: Reword to “harness with zero bundled rules.”
- Release impact: Accept for 0.0.1
- Status: open

### Issue 6 -- Severity: suggestion
- File: `rules/grok-build/*.json` source `url` fields
- Description: Citations use `blob/main/…` while notes pin `SOURCE_REV`. Integrity is preserved via `content_hash` + notes; main can drift from the hash (doc-drift is the intended catch once enabled).
- Suggestion: Optionally pin blob URLs to `124d85bc…` for permanence.
- Release impact: Accept for 0.0.1
- Status: open

### Issue 7 -- Severity: suggestion
- File: `crates/harness-guard-rules/tests/schema_validation.rs:330-338`
- Description: Load test still asserts weak minima (`rules.len() >= 3`, codex `>= 3`). Silent drop of all Grok rules would fail capabilities golden / fixture matrix, not this unit test. Prior review noted the same.
- Suggestion: Assert total ≥ 13 / claude ≥ 5 / codex ≥ 4 / grok ≥ 4 (or exact counts).
- Release impact: Accept for 0.0.1
- Status: open

### Issue 8 -- Severity: nit
- File: `crates/harness-guard-cli/tests/cli_surface.rs` (explain tests)
- Description: `explain` is golden-tested for codex + claude rules; no Grok rule explain path test. Covered by load/schema/fixtures for content.
- Suggestion: Optional parity `explain grok-build-telemetry-01`.
- Release impact: Accept for 0.0.1
- Status: open

### Issue 9 -- Severity: nit
- File: `crates/harness-guard-core/src/harness.rs:91` (`path_binary: Some("grok")`)
- Description: Evidence notes PATH alias `agent`; descriptor only detects `grok`. Miss → home-only or no version — conservative, not false confidence.
- Suggestion: Document in agent-guide/detection notes; add `agent` only with evidence if desired later.
- Release impact: Accept for 0.0.1
- Status: open

### Issue 10 -- Severity: nit
- File: `docs/superpowers/plans/2026-07-16-harness-guard-0.0.1-multi-harness.md:29` (Global Constraints retired-key ban)
- Description: Plan text still forbids reappearance of `trace_upload` / `[telemetry]` etc. Owner decision + protocol §7 + tripwires supersede this for live keys. Product code/rules correctly follow the supersession; plan text is stale.
- Suggestion: Append a dated correction note to the plan (no need to rewrite history).
- Release impact: Accept for 0.0.1
- Status: open

## Outstanding Minors triage (from handoff)

| item | status | recommendation |
| --- | --- | --- |
| `walk_files` bare `.unwrap()` in tripwire helper | still open | **Accept for 0.0.1** — test-only, fail-closed |
| Validator error messages omit offending value; `validate_integer_partition` doesn’t reuse `value_sets()`; no corpus for `integer_bounds` on non-int; wildcard match arm | still open | **Accept for 0.0.1** — no shipped-rule impact |
| Stale plan-mandated comment at `parse.rs:79` re out-of-i64 | **superseded** | **Closed** — current comment documents `Int(i64)` / Other correctly |
| Engine `Bool`/`Int` render arms untested | **superseded** | **Closed** — bool rules (codex + grok) and integer cleanup-period are exercised by goldens |
| `engine.rs` coverage comment misattributes Unparseable; Task 7 report wrong test cite | still open (doc) | **Accept** — no behavior impact |
| `EMBEDDED_RULES` could be `pub(crate)`; tree-wide `ruleset.json` name exclusion | still open | **Accept** — brief design |
| Hostility `config_states` omits `NotRegularFile`/`Io` as explicit states; assertion msgs lack context | still open | **Accept** — covered indirectly |
| TOML-disable guard comment compresses two-step mechanism | still open | **Accept** — convention |
| `HarnessId` derived `Ord` vs `as_str()` alpha coupling untested | partially mitigated | **Accept** — `ALL` alphabetical test + zip `debug_assert` |
| `CLAUDE_CONFIG_DIR` real-but-undocumented, unwired | still open | **Accept** — intentional official-docs rule; cited in `main.rs` |
| `capabilities` `commands` + `report_schema_version` hardcoded | still open | **Accept** — plan-mandated; golden pins schema version |

**New from this review:** Issues 1–10 above; only Issue 1 is “fix before tag preferred.”

## Grok support assessment

| Dimension | Parity vs Claude/Codex | Notes |
| --- | --- | --- |
| Detection | On par / stronger | npm + managed-install basename fallback; `GROK_HOME`; no `agent` alias |
| Rule count / categories | Parity-by-rigor (spec §7) | 4 rules: telemetry, feedback, transfer (feedback), otel prompt log — no retention key found |
| Evidence class | Correctly weaker on behavior | Local posture / official-documentation only; no lab, no canary claims |
| Fixtures / hostility | On par | 14 cases + runtime managed-install + symlink/oversized/perm |
| Version pin | 0.2.102 | Channel + npm + SOURCE_REV aligned |
| Gaps post-0.0.1 | Lab path | Wire behavior / canary; optional mixpanel rule; tighter version min; commit-pinned source URLs; `agent` alias; optional `explain` golden |

Honest product posture: Grok is a co-equal **local config auditor**, not a behavioral exfil scanner. That matches owner decisions and avoids the false-confidence trap the original clean-room gate was meant to prevent.

## Test / gate confidence

**Verified by reading (this review):**
- Architecture paths: `harness.rs`, `discovery.rs`, `version.rs`, `engine.rs`, `scan.rs`, `readfs.rs` (policy), `parse.rs`, CLI `main.rs` / `redact.rs` / `capabilities.rs` / `render_term.rs`, rules loader totality hooks, tripwires, all four Grok rules, evidence pack + protocol, no-egress script multi-harness matrix, capabilities golden, version `0.0.1`, freshness gate-off, release checklist.
- Safety matrix: core ambient bans present; tests use `env_clear` + synthetic fixtures; Grok/Claude/Codex real homes not used; `network_requests_made: 0` hardcoded; no `independent-reproduction` in rules; canary claim ban present.
- Re-checked prior final review findings; none overturned as under-severity.

**Not re-executed in this review session:**
- Full `cargo fmt/clippy/deny/test` and `scripts/no-egress/run-macos.sh` (reported green at prior tip; **must re-run on exact tag commit** per Task 25).
- `actionlint` on workflow changes (release-watch gained Grok probe under the existing gate).

**Residual risk:** Tagging without re-running gates; CHANGELOG overstatement if left unfixed; Grok `<=` range over-applying confident findings to very old installs; managed-install existence check uses raw `std::fs` (by design, version-only, not config-path policy).
